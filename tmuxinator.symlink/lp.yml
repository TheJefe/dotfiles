# ~/.tmuxinator/lp.yml

name: lesson-player
root: ~/git/lesson-player

# Runs in each window and pane before window/pane specific commands. Useful for setting up interpreter versions.
#pre_window: rvm use .

windows:
  - editor:
  - server: git pull --no-edit; npm install; grunt install; grunt serve:dist